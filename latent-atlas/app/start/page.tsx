import type {Metadata} from 'next';
import {RoutesPage} from '@/components/atlas/pages';
export const metadata:Metadata={title:'Where to start | Ghost in the Fold',description:'Three guided routes through protein language models, structure prediction and design.'};
export default function Page(){return <RoutesPage/>}
