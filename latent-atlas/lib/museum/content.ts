export const links=(slug:string)=>slug==='map'?'/':`/exhibits/${slug}`;
export const exhibits = [
 {slug:'map',title:'The big picture',short:'The big picture',kicker:'Protein language models, unfolded',heading:'Attention is all you need to',accent:'fold proteins.',description:'For beginners in protein design, and for anyone who forgets the basics. This is the closest we have come to what nature does in seconds, and what nature does is still the better-kept secret. Start with the mechanism, then read the models that use it.',minutes:4},
 {slug:'classical',title:'Learning from data',short:'Classical machine learning',kicker:'01 / Classical machine learning',heading:'Patterns,',accent:'not instructions.',description:'Give a model examples, and it learns a rule. Change the problem and see where a simple boundary succeeds or fails.',minutes:6},
 {slug:'neuron',title:'Inside a neuron',short:'The perceptron',kicker:'02 / The building block',heading:'One small',accent:'decision.',description:'Inputs carry information. Weights decide how much it matters. Adjust them to see a neuron compute its output.',minutes:5},
 {slug:'deep',title:'When layers learn',short:'Deep learning',kicker:'03 / Deep learning',heading:'Simple parts.',accent:'Complex patterns.',description:'Connect neurons into layers, then train their weights. Watch a network bend its decision boundary around a nonlinear problem.',minutes:7},
 {slug:'language',title:'Learning a language',short:'Language models & BERT',kicker:'04 / Language models',heading:'The meaning',accent:'between tokens.',description:'A language model learns statistical patterns in sequences. Compare next-token prediction with BERT’s masked-token objective.',minutes:7},
 {slug:'attention',title:'Paying attention',short:'Attention & Transformers',kicker:'05 / Attention & Transformers',heading:'Context changes',accent:'everything.',description:'A token does not have to work alone. Inspect how queries, keys and values let it exchange information with the rest of a sequence.',minutes:9},
 {slug:'protein',title:'The language of life',short:'Protein language models',kicker:'06 / Protein language models',heading:'New alphabet.',accent:'Shared ideas.',description:'Replace words with amino acids. Explore how learning from protein sequences can produce representations useful for biology.',minutes:7},
 {slug:'finetuning',title:'From general to specific',short:'The fine-tuning lab',kicker:'07 / The fine-tuning lab',heading:'A new task.',accent:'A head start.',description:'Reuse what a model has learned, then adapt it to a specific task. Compare a frozen encoder, full fine-tuning and low-rank adaptation.',minutes:9},
];
export const sources = {
 transformer:{name:'Vaswani et al. · Attention Is All You Need (2017)',url:'https://arxiv.org/abs/1706.03762'},
 bert:{name:'Devlin et al. · BERT (2018)',url:'https://arxiv.org/abs/1810.04805'},
 esm:{name:'Lin et al. · ESM-2 & ESMFold (2023)',url:'https://www.science.org/doi/10.1126/science.ade2574'},
 prottrans:{name:'Elnaggar et al. · ProtTrans',url:'https://arxiv.org/abs/2007.06225'},
 lora:{name:'Hu et al. · LoRA (2021)',url:'https://arxiv.org/abs/2106.09685'},
 dl:{name:'Goodfellow, Bengio & Courville · Deep Learning (2016)',url:'https://www.deeplearningbook.org/'},
 esmcode:{name:'ESM · Official code and model documentation',url:'https://github.com/facebookresearch/esm'},
};
export const details:Record<string,{plain:string;technical:string;biology:string;equation:string;extra:string[];refs:(keyof typeof sources)[];question:string;options:string[];correct:number;feedback:string}>={
 map:{plain:'Artificial intelligence is the broad field. Machine learning is an approach within it: learning patterns from data rather than specifying every rule.',technical:'Deep learning is a subset of machine learning based on multilayer neural networks. Transformers are one architecture; language modeling is an objective. These are related categories, not synonyms.',biology:'Protein language models apply sequence-learning objectives to amino acids. Not every protein model predicts a 3D structure, and not every language model is a Transformer.',equation:'AI ⊃ ML ⊃ DL',extra:['Classical machine learning includes logistic regression, decision trees, random forests and support vector machines. “Classical” does not necessarily mean linear, simple or obsolete.','A large language model (LLM) is a language model at large scale; there is no universal parameter threshold. A protein language model (pLM, sometimes called a PLLM) models amino-acid sequences. Here PLLM always means protein language model, not simply pretrained language model.','Follow the numbered galleries for a guided visit, or select any node in the map. Hover, click, touch and keyboard focus reveal the same information. All live experiments use compact educational models, not hosted foundation models.'],refs:['dl','transformer','esm'],question:'Which relationship is correct?',options:['Deep learning is a subset of ML','All AI is a Transformer','Every pLM predicts 3D structure'],correct:0,feedback:'Deep learning is part of machine learning. A Transformer is a particular neural architecture, and protein structure prediction needs an appropriate model and output head.'},
 classical:{plain:'Imagine sorting observations into two groups. Training finds parameters that reduce mistakes on examples whose labels are already known.',technical:'This experiment fits logistic regression to two numerical inputs using binary cross-entropy and batch gradient descent. Its probability boundary at 0.5 is a straight line. XOR requires a nonlinear representation.',biology:'A classical protein model might use charge, length or amino-acid composition as features. A random forest or a kernel SVM can also model nonlinear relationships without being a deep network.',equation:'p(y=1|x) = σ(w·x + b)',extra:['For each example, compute a probability, measure cross-entropy loss against its label, differentiate the loss and update the weights. One epoch here is one pass over the entire fixed training set.','The visual uses 100 synthetic training points and 60 independently generated validation points. Both are sampled from a chosen rule. The boundary heatmap shows predicted class probability; the symbols show training labels. Validation accuracy is separate from training loss.','A random split of highly similar proteins can leak family information between sets. For biological studies, consider sequence-identity or family-aware splits, an untouched test set, class imbalance and appropriate baselines. Accuracy alone can be misleading.'],refs:['dl'],question:'Why can logistic regression struggle on XOR?',options:['It needs more decimal places','Its boundary in these features is linear','It has no trainable weights'],correct:1,feedback:'The opposite quadrants of XOR cannot be separated by one straight line. Feature engineering or a nonlinear model can change that.'},
 neuron:{plain:'A neuron multiplies each input by a weight, adds a bias, and applies an activation function. A weight can strengthen an input or reverse its effect.',technical:'The original perceptron uses a hard threshold and a mistake-driven learning rule. A sigmoid neuron is a differentiable alternative; it is not identical to the historical perceptron.',biology:'In a simple protein classifier, two inputs could encode normalized hydrophobicity and charge. The numbers in this exhibit are generic features, not a validated biological prediction.',equation:'z = w₁x₁ + w₂x₂ + b; a = f(z)',extra:['A positive weight increases z when its input increases. A negative weight decreases z. The bias shifts the response even when both inputs are zero.','Threshold outputs 1 for z ≥ 0 and 0 otherwise. Sigmoid maps z to (0,1). ReLU returns max(0,z), so its output is not inherently a probability.','Backpropagation uses the chain rule to compute derivatives through differentiable operations. A hard step is not useful for ordinary gradient-based backpropagation; the original perceptron uses a different update rule.'],refs:['dl'],question:'What does the bias change?',options:['The number of inputs','The activation’s offset','The training dataset'],correct:1,feedback:'The bias shifts the pre-activation z independently of the current inputs, moving the decision threshold.'},
 deep:{plain:'One layer transforms the inputs; the next works with the transformed features. Nonlinear activations allow these compositions to represent more complex patterns.',technical:'This lab trains a 2 → 6 → 1 multilayer perceptron with tanh hidden activations and a sigmoid output. It is a small neural-network demonstration; modern deep models have many more layers.',biology:'A deep protein model can learn sequence representations instead of relying only on hand-designed descriptors. Architecture and training objectives determine what information those representations retain.',equation:'h = tanh(W₁x + b₁); p = σ(W₂h + b₂)',extra:['Forward pass: compute hidden activations and predictions. Loss: compare predictions with the target labels. Backward pass: propagate loss derivatives to all trainable parameters. Update: θ ← θ − η∇θL.','Without nonlinear activations, a stack of affine layers is still an affine mapping. Depth alone does not make a nonlinear decision boundary. This experiment uses tanh to avoid that collapse.','The curves come from actual optimization in your browser. Training and validation use different synthetic examples. A low training loss does not establish biological usefulness or performance on unseen protein families.'],refs:['dl'],question:'What happens if all hidden activations are linear?',options:['The network becomes a Transformer','The composition stays affine','It learns any nonlinear boundary'],correct:1,feedback:'Composing affine transformations produces another affine transformation. Nonlinear activations enable nonlinear representations.'},
 language:{plain:'Tokenization turns a sequence into discrete symbols. An embedding maps each symbol to a vector. A language model learns to assign probabilities using context.',technical:'Autoregressive models predict the next token from earlier tokens. BERT is an encoder that learns bidirectional representations using masked language modeling; original BERT also used next-sentence prediction.',biology:'BERT makes a useful bridge to masked protein modeling: hide part of a sequence and predict the missing residue from both sides. The general objective transfers, although tokenization and data differ.',equation:'Lnext = −Σ log p(xₜ | x<t)',extra:['BERT-base has 12 Transformer encoder layers, 768 hidden dimensions and 12 attention heads, with about 110 million parameters. It is a useful introductory reference architecture, but not a primitive perceptron or a chat model.','In original BERT, 15% of token positions are selected for the masked objective. Of those selected positions, 80% become [MASK], 10% become a random token, and 10% are left unchanged. Loss is computed on the selected targets.','The interactive probability bars use a tiny count-based corpus. Next-token mode uses the preceding word; masked mode uses adjacent left/right words when a context matches, otherwise unigram counts. Additive smoothing and temperature turn counts into probabilities. This is not BERT inference.','Text tokenization here splits words; actual BERT uses WordPiece subwords. During autoregressive generation, temperature changes the output distribution and sampling chooses a next token. That is inference, not a training-weight update.'],refs:['bert','transformer'],question:'What context can BERT’s encoder use for a masked token?',options:['Only tokens to its left','Only the masked token itself','Tokens on both sides'],correct:2,feedback:'Bidirectional self-attention can use the left and right context. A causal decoder blocks access to future positions.'},
 attention:{plain:'For each token, attention asks which other tokens are useful now. It computes a weighted mixture of their information, producing a context-aware representation.',technical:'Q = XWQ, K = XWK, V = XWV. Query-key dot products are scaled by √dk, optionally masked, then normalized row-wise with softmax. Each row of weights sums to one.',biology:'Residue attention can encode sequence dependencies, including long-range ones. An attention value is not automatically a physical contact, a causal explanation or proof of biochemical interaction.',equation:'Attention(Q,K,V) = softmax(QKᵀ/√dₖ + M)V',extra:['The heatmap uses deterministic, untrained 4-dimensional token embeddings with sinusoidal position information. Each selectable head uses different fixed 4×4 projections. These are exact attention calculations on toy vectors, not extracted ESM or BERT weights.','A causal mask sets future-position scores to negative infinity before softmax, making their probabilities zero. An encoder uses bidirectional context; a decoder-only language model uses a causal mask.','Multi-head attention concatenates each head’s result and projects it back to the model dimension. A Transformer block also includes a position-wise feed-forward network, residual connections and normalization. Stacking attention alone does not describe the whole architecture.','The original Transformer is an encoder-decoder model. BERT uses the encoder design; GPT-style models use causal decoder blocks. Normalization placement and positional mechanisms vary across model families. Standard dense attention creates an n×n interaction matrix per head, with quadratic sequence-length cost.'],refs:['transformer','bert'],question:'Why does one attention row sum to 1?',options:['Softmax normalizes the row','All tokens have equal importance','It measures physical distance'],correct:0,feedback:'Softmax makes the unmasked entries nonnegative and normalizes their sum to one. Masked entries have zero weight.'},
 protein:{plain:'A protein sequence is a chain of amino acids, often written with a 20-letter alphabet. A pLM learns statistical regularities across many such sequences.',technical:'Masked pLMs such as ESM-2 learn contextual residue embeddings. A representation can be pooled into one vector per sequence, or passed to a residue-level prediction head. ProtT5 is part of the ProtTrans family.',biology:'Possible downstream tasks include localization, secondary structure, stability and function prediction. Sequence representations are useful inputs, but a task still needs suitable labels, evaluation and biological interpretation.',equation:'Lmask = −Σᵢ∈M log p(aᵢ | a_visible)',extra:['Select a residue to mask it. The feature inspector reports its identity and simple chemical grouping; it does not predict which amino acid belongs at the masked position. Hydrophobic fraction is a descriptor, not a solubility probability.','ESM-2 is a protein language model. ESMFold combines ESM-2 representations with a folding architecture to predict structure. The language model alone is not the complete structure predictor. ProtTrans includes several protein language models, including ProtBERT and ProtT5.','A residue embedding is a learned vector, not a named list of physical properties. Mean pooling averages vectors over valid residues, excluding padding and special tokens, and loses some positional information. For residue-level tasks, retain per-residue representations.','Pretraining learns from large unlabeled sequence collections via self-supervised targets. Evolutionary correlations may help representations capture structure and function, but sequence redundancy, dataset bias and out-of-distribution sequences limit generalization.'],refs:['esm','esmcode','prottrans'],question:'Does ESM-2 alone equal ESMFold?',options:['Yes, they are interchangeable','No, ESMFold adds a folding architecture','Only for short sequences'],correct:1,feedback:'ESM-2 provides learned sequence representations. ESMFold adds the machinery that converts them into a structure prediction.'},
 finetuning:{plain:'Pretraining learns reusable representations. To solve task X, add an output head and train it with task-specific labels. You can also update some or all of the pretrained model.',technical:'A linear probe freezes the encoder and trains a head. Full fine-tuning updates both. LoRA freezes original weights and learns low-rank changes ΔW = BA in selected matrices.',biology:'For protein solubility prediction, a possible pipeline is sequence → pretrained encoder → pooled embedding → binary head. Use experimentally grounded labels, family-aware splits and a held-out test set before interpreting performance.',equation:'Wadapted = Wfrozen + (α/r)BA',extra:['The live experiment runs a tiny two-input, six-hidden-unit network. It first learns a synthetic source task (x₁ + 0.6x₂ > 0), resets the output head, then learns a synthetic target task (x₁x₂ < 0). Both tasks use synthetic coordinates, not real proteins or solubility labels.','Frozen mode updates seven output-head parameters. Full mode updates all 25 parameters. Low-rank mode updates rank-1 factors A and B around the frozen 6×2 input weight matrix, plus the seven head parameters: 15 trainable parameters. The low-rank scale is one in this demo.','Each run reuses the same source pretraining and target split so strategy comparisons are controlled. Its validation score measures only the synthetic target task. Larger models, real data and different optimizers can change the relative performance.','For a real pLM workflow: deduplicate and cluster sequences; reserve an untouched test set; fit preprocessing only on training data; tune learning rate and stopping on validation; compare against a descriptor baseline and frozen embeddings; report uncertainty, class-sensitive metrics and external validation when possible.','Fine-tuning is not prompting, retrieval-augmented generation or ordinary inference. It changes trainable parameters. Large pLM training requires an external Python/GPU environment; this museum intentionally runs only the small educational experiments.'],refs:['lora','esmcode','prottrans'],question:'What is updated in a frozen-encoder linear probe?',options:['The entire pretrained encoder','Only the task head','No parameters at all'],correct:1,feedback:'A linear probe trains a new head while preserving encoder parameters. Full fine-tuning and low-rank adaptation modify additional trainable components.'},
};
